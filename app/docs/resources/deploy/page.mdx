# Deploy Checklist

Before shipping to production:

- Run `npm run lint`
- Build docs with `npm run build`
- Validate server startup logs
- Confirm critical events and commands in staging

## Rollback

Keep a rollback artifact and last-known-good resource bundle available.
