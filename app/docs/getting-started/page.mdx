# Installation

## Prerequisites

- Node.js 20+
- A working RedM server artifacts setup
- Access to your resources repository

## Local setup

1. Install dependencies with `npm install`
2. Start the docs app with `npm run dev`
3. Keep docs updates in the same pull request as script changes

## Suggested structure

Keep environment setup scripts and docs in predictable locations so onboarding is fast.
